# Scripts

`start-*.sh` runs `bench compose up`, which syncs host `apps/` into named Docker volumes before starting containers.

- `./start-local-dev.sh` / `./stop-local-dev.sh`
- `./start-remote-prod.sh` / `./stop-remote-prod.sh`
