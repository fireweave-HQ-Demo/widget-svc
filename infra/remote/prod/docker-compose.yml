# Generated by fireweave test-bench
# workspace=stg_dd_react_ts bucket=remote env=prod
# destination=datadog
# services: <app>-api, <app>-web, collector (observability)
name: bench-stg_dd_react_ts-remote-prod
services:
  atlas-api:
    # api — atlas (bun+react)
    image: oven/bun:1.2-alpine
    working_dir: /app
    environment:
      APP_ENV: prod
      PORT: "3000"
      BENCH_DESTINATION: datadog
      OTEL_SERVICE_NAME: atlas-api
      OTEL_EXPORTER_OTLP_ENDPOINT: http://collector:4318
      OTEL_RESOURCE_ATTRIBUTES: service.namespace=stg_dd_react_ts,deployment.environment=prod
    ports:
      - "0.0.0.0:18050:3000"
    volumes:
      - bench-stg_dd_react_ts-atlas-api:/app
    networks: [bench]
    depends_on: [collector]
    command: ["bun","run","start"]
    restart: unless-stopped
  atlas-web:
    # web — atlas (bun+react)
    image: oven/bun:1.2-alpine
    working_dir: /app
    environment:
      APP_ENV: prod
      PORT: "5173"
      BENCH_DESTINATION: datadog
      OTEL_SERVICE_NAME: atlas-web
      OTEL_EXPORTER_OTLP_ENDPOINT: http://collector:4318
      OTEL_RESOURCE_ATTRIBUTES: service.namespace=stg_dd_react_ts,deployment.environment=prod
      VITE_SERVICE_NAME: atlas-web
      VITE_APP_ENV: prod
      VITE_DESTINATION: datadog
      VITE_API_PORT: "18050"
      VITE_OTEL_PORT: "18052"
    ports:
      - "0.0.0.0:18051:5173"
    volumes:
      - bench-stg_dd_react_ts-atlas-web:/app
    networks: [bench]
    depends_on: [collector]
    command: ["sh","-c","bun run build && bun run preview -- --host 0.0.0.0 --port 5173"]
    restart: unless-stopped
  collector:
    # observability — OTLP → datadog
    image: otel/opentelemetry-collector-contrib:0.120.0
    command: ["--config=/etc/otelcol-contrib/config.yaml"]
    ports:
      - "0.0.0.0:18052:4318"
      - "0.0.0.0:18053:13133"
    env_file:
      - path: /Users/kalyanchowdary/Desktop/code/work/test-bench/bench/credentials/datadog
        required: false
    volumes:
      - bench-stg_dd_react_ts-collector:/etc/otelcol-contrib
    networks: [bench]
    restart: unless-stopped
networks:
  bench:
    name: bench-stg_dd_react_ts-remote-prod-net
volumes:
  bench-stg_dd_react_ts-atlas-api:
    external: true
  bench-stg_dd_react_ts-atlas-web:
    external: true
  bench-stg_dd_react_ts-collector:
    external: true
