# Generated by fireweave test-bench
# workspace=demo_test bucket=local env=dev
# destination=openobserve (local hot-reload)
# services: <app>-api, <app>-web, collector (observability)
name: bench-demo_test-local-dev
services:
  atlas-api:
    # api — atlas (bun+react)
    image: oven/bun:1.2-alpine
    working_dir: /app
    environment:
      APP_ENV: dev
      PORT: "3000"
      BENCH_DESTINATION: openobserve
      OTEL_SERVICE_NAME: atlas-api
      OTEL_EXPORTER_OTLP_ENDPOINT: http://collector:4318
      OTEL_RESOURCE_ATTRIBUTES: service.namespace=demo_test,deployment.environment=dev
      IDENTITY_ENABLED: "true"
      IDENTITY_SEED_PATH: /data/identity/seed.json
    ports:
      - "127.0.0.1:18000:3000"
    volumes:
      - bench-demo_test-atlas-api:/app
      - bench-demo_test-identity:/data/identity:ro
    networks: [bench]
    depends_on: [collector]
    command: ["bun","--hot","run","src/main.ts"]
    restart: unless-stopped
  atlas-web:
    # web — atlas (bun+react)
    image: oven/bun:1.2-alpine
    working_dir: /app
    environment:
      APP_ENV: dev
      PORT: "5173"
      BENCH_DESTINATION: openobserve
      OTEL_SERVICE_NAME: atlas-web
      OTEL_EXPORTER_OTLP_ENDPOINT: http://collector:4318
      OTEL_RESOURCE_ATTRIBUTES: service.namespace=demo_test,deployment.environment=dev
      VITE_SERVICE_NAME: atlas-web
      VITE_APP_ENV: dev
      VITE_DESTINATION: openobserve
      VITE_API_BASE: http://127.0.0.1:18000
      VITE_OTEL_ENDPOINT: http://127.0.0.1:18002
      VITE_IDENTITY_ENABLED: "true"
    ports:
      - "127.0.0.1:18001:5173"
    volumes:
      - bench-demo_test-atlas-web:/app
      - bench-demo_test-identity:/data/identity:ro
    networks: [bench]
    depends_on: [collector]
    command: ["bun","run","dev","--","--host","0.0.0.0","--port","5173"]
    restart: unless-stopped
  collector:
    # observability — OTLP → openobserve
    image: otel/opentelemetry-collector-contrib:0.120.0
    command: ["--config=/etc/otelcol-contrib/config.yaml"]
    ports:
      - "127.0.0.1:18002:4318"
      - "127.0.0.1:18003:13133"
    env_file:
      - path: /Users/kalyanchowdary/Desktop/code/work/test-bench/bench/credentials/openobserve
        required: false
    volumes:
      - bench-demo_test-collector:/etc/otelcol-contrib
    networks: [bench]
    restart: unless-stopped
networks:
  bench:
    name: bench-demo_test-local-dev-net
volumes:
  bench-demo_test-atlas-api:
    external: true
  bench-demo_test-atlas-web:
    external: true
  bench-demo_test-collector:
    external: true
  bench-demo_test-identity:
    external: true
