# Collector (control)

OTLP HTTP :4318. Active pipelines use **debug** so the collector always boots.
Vendor wiring for **control** is documented in `vendor.exporter.yaml`.
Config is seeded into `bench-sample-atlas-collector` and used by env compose under `infra/`.
