# Bench workspace — stg_polyglot

This branch `bench/stg_polyglot` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/stg_polyglot/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.git.
