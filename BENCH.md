# Bench workspace — sample-delta

This branch `bench/sample-delta` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/sample-delta/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.
