# Bench workspace — demo_sample

This branch `bench/demo_sample` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/demo_sample/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.
