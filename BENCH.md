# Bench workspace — widget-atlas-1

This branch `bench/widget-atlas-1` (from `main`) was **force-pushed** by the Fireweave test bench with fixture template code + agent settings.
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.git.
