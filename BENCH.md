# Bench workspace — demo_test

This branch `bench/demo_test` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/demo_test/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.
