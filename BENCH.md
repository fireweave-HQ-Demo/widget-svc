# Bench workspace — prod_oodle_bench

This branch `bench/prod_oodle_bench` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/prod_oodle_bench/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.git.
