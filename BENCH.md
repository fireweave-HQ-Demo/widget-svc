# Bench workspace — rt0

This branch `bench/rt0` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/rt0/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.git.
