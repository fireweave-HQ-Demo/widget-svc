# Bench workspace — verify_git_a

This branch `bench/verify_git_a` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/verify_git_a/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.
