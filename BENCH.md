# Bench workspace — test_sample_z

This branch `bench/test_sample_z` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/test_sample_z/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.
