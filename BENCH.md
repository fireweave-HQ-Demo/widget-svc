# Bench workspace — oodle_react_ts

This branch `bench/oodle_react_ts` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/oodle_react_ts/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.git.
