# Bench workspace — stg_oodle_react_py

This branch `bench/stg_oodle_react_py` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/stg_oodle_react_py/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.git.
