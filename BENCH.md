# Bench workspace — sample-atlas

This branch `bench/sample-atlas` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/sample-atlas/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.
