# Bench workspace — verify_git_b

This branch `bench/verify_git_b` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/verify_git_b/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.
