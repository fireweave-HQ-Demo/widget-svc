# Bench workspace — sample-harbor

This branch `bench/sample-harbor` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/sample-harbor/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.
