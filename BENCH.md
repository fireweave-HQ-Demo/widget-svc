# Bench workspace — stg_bench

This branch `bench/stg_bench` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/stg_bench/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.git.
