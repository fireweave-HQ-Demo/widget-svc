# Bench workspace — sample_skills_git

This branch `bench/sample_skills_git` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/sample_skills_git/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.
