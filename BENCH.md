# Bench workspace — tue_test

This branch `tue_test` (from `main`) was **force-pushed** by the Fireweave test bench with the full workspace tree (`bench/workspaces/tue_test/`).
It does not keep the previous contents of https://github.com/fireweave-HQ-Demo/widget-svc.git.
